# 打包部署指南

## 一、打包前端项目

### 1. 安装依赖（如果还没安装）
```bash
npm install
```

### 2. 打包项目
```bash
npm run build
```

打包完成后，会在项目根目录生成一个 `dist` 文件夹，这就是打包好的静态文件。

---

## 二、分发方式

### 方式1：直接发送 dist 文件夹（最简单）
1. 把 `dist` 文件夹压缩成 zip 文件
2. 发送给朋友
3. 朋友解压后，用浏览器打开 `dist/index.html` 即可使用

### 方式2：部署到 GitHub Pages（免费、推荐）
1. 在 GitHub 创建一个新仓库
2. 把 `dist` 文件夹里的内容上传到仓库
3. 在仓库设置 -> Pages 中选择分支和目录
4. 几分钟后就会有一个访问链接

### 方式3：使用 Vercel（最简单的自动部署）
1. 注册 Vercel 账号
2. 把代码推送到 GitHub
3. 在 Vercel 导入项目
4. 自动部署完成！

### 方式4：本地启动服务器测试
如果你想在本地测试打包后的文件，可以用以下方式：

#### 使用 Python（如果有安装 Python）
```bash
cd dist
python -m http.server 8080
```
然后在浏览器打开 `http://localhost:8080`

#### 使用 Node.js 的 http-server
```bash
npm install -g http-server
cd dist
http-server -p 8080
```

---

## 三、注意事项

### 关于后端
**重要提醒：** 目前后端是运行在本地的，如果想让别人也能用完整功能（登录、聊天等），你需要：
1. 把后端也部署到服务器
2. 或者你们在同一个局域网内使用

### 快速预览（仅前端）
如果只是想让别人看看界面，可以直接发送 dist 文件夹，AI 功能和基础界面都能用！

---

## 四、常见问题

**Q: 打包后页面空白怎么办？**
A: 检查 vite.config.js 中的 base 配置，确保是 './'

**Q: 发送给别人后 AI 功能不能用？**
A: 检查 API Key 是否在代码中配置了，如果是写死的可以用；如果是 localStorage 存储的，需要重新配置

**Q: 图片无法加载？**
A: 确保图片路径是相对路径，或者用 CDN 链接
