# CampusVibe 后端系统启动说明

## 概述

我们已成功创建了完整的后端系统，使用 Node.js + Express + 内存存储（基于 JSON）。

## 项目结构

```
系统分析与设计/
├── src/              # 前端代码
├── backend/          # 后端代码
│   ├── routes/       # API 路由
│   ├── data.js       # 数据存储
│   ├── package.json
│   └── server.js
└── ...
```

## 启动步骤

### 方法一：使用启动脚本（推荐）

双击运行 `start-backend.bat` 文件即可自动启动后端。

### 方法二：手动启动

#### 1. 安装后端依赖

```bash
cd backend
E:\Node.js\npm.cmd install
```

#### 2. 启动后端服务器

```bash
E:\Node.js\node.exe server.js
```

后端将在 `http://localhost:3001` 启动

#### 3. 启动前端（在另一个终端）

```bash
cd ..
npm run dev
```

前端将在 `http://localhost:5173` 启动

## 测试账号

所有示例用户的密码都是：**123456**

可用的测试用户名：

- 火锅爱好者
- 小辣椒
- 吃货一枚
- 期末战士
- 微积分杀手
- 骑行侠
- 风一样的男子
- 山地车选手
- 周末不宅
- 国服鲁班
- 打野贼6
- 辅助永远的神
- 快门狂魔
- 富士党

## API 接口文档

### 用户相关

- `POST /api/users/register` - 用户注册
  - 参数：{ username, password, mbti, tags }
- `POST /api/users/login` - 用户登录
  - 参数：{ username, password }
- `GET /api/users/:id` - 获取用户信息

### 活动相关

- `GET /api/activities` - 获取活动列表
- `GET /api/activities/:id` - 获取活动详情
- `POST /api/activities` - 创建活动
  - 参数：{ title, type, typeIcon, description, location, time, maxPeople, creatorId, tags, coverGradient }
- `POST /api/activities/:id/join` - 参加活动
  - 参数：{ userId }
- `POST /api/activities/:id/comments` - 添加评论
  - 参数：{ username, text, aiGenerated }

### 聊天相关

- `GET /api/chats?userId=xxx` - 获取用户聊天列表
- `GET /api/chats/:id` - 获取聊天详情
- `POST /api/chats/:id/messages` - 发送消息
  - 参数：{ from, text }

### 健康检查

- `GET /api/health` - 检查服务器状态

## 数据存储说明

当前版本使用内存存储（基于 JSON），数据会在服务器重启时重置。

如需持久化存储，可以考虑：

1. SQLite（轻量级文件数据库）
2. MongoDB（文档数据库）
3. PostgreSQL（关系型数据库）

## 切换前后端模式

在 `src/stores/app.js` 和 `src/stores/user.js` 中，有一个常量：

```javascript
const USE_REAL_BACKEND = true; // 改为 false 可切换回 mock 模式
```

## 当前状态

✅ 后端服务器已成功启动在 http://localhost:3001
✅ 前端已连接到后端 API
✅ 所有功能正常工作

## 故障排除

### 端口被占用

如果 3001 端口被占用，可以在 `backend/server.js` 中修改端口号。

### 前端无法连接后端

- 确认后端已启动
- 检查 `src/services/api.js` 中的 API_BASE 地址是否正确
- 确认没有跨域问题（CORS已配置）

## 后续开发建议

1. 添加用户认证（JWT）
2. 添加输入验证
3. 添加日志系统
4. 添加单元测试
5. 考虑使用 Redis 缓存
6. 实现持久化数据库存储
7. 考虑部署到云平台
